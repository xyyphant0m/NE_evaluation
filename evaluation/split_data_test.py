import numpy as np
import os
from utils import *

if __name__=="__main__":
    dataset_name = "cora"

    hide_links(dataset_name,ratio = 0.5)
    #hide_links(dataset_name,ratio = 0.6)
    hide_links(dataset_name,ratio = 0.7)
    #hide_links(dataset_name,ratio = 0.8)
    #hide_links(dataset_name,ratio = 0.9)



